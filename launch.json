{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-node",            // ou "node" em versões mais antigas
      "request": "launch",
      "name": "Launch src/server.js",
      "program": "${workspaceFolder}/src/server.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
